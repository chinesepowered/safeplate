/** Per-app identity. The only file that differs between our hackathon repos. */
export const APP_SLUG = "safeplate";
/** Prefix for per-case email routing codes, e.g. SP-7F3K. */
export const CASE_PREFIX = "SP";
export const APP_NAME = "SafePlate";
